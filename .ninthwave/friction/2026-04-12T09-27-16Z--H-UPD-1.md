item: H-UPD-1
date: 2026-04-12T09:27:16Z
severity: high
harness: opencode with gpt5.4 high model (task subsequently continued to completion by claude code with opus 4.6)
description: After creating PR #644, this worker incorrectly exited back to user-facing mode instead of entering the mandatory drain-wait inbox loop from implementer.md. Root cause: I treated PR creation as the completion boundary and answered with the PR URL rather than continuing the non-interactive post-PR lifecycle. Impact: the orchestrator's review-feedback message sat unhandled until the user manually prompted an inbox check, delaying the required feedback fix loop. Context: the original post-PR exit occurred under the opencode/gpt5.4-high harness; the follow-up fix/review/feedback loop (including this friction entry update) was handled by claude code opus 4.6 -- the `implementer.md` post-PR contract needs to be robust across harnesses so workers do not rely on a specific model's instruction-following to stay in the inbox loop.
