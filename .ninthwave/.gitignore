# Deny by default -- only explicitly allowed files are committed
*

# Committed project files
!.gitignore
!config.json
# work-item-format.md is intentionally NOT allowlisted in this dogfood repo:
# the canonical source lives at core/docs/work-item-format.md, and the copy
# under .ninthwave/ is a scaffold-generated mirror that `nw init` writes.
# Consumer repos DO allowlist it (see the template in
# core/commands/init.ts), because they don't have the canonical source.
!work/
!work/**
!friction/
!friction/**
!decisions/
!decisions/**
